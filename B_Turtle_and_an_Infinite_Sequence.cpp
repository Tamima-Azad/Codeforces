#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    ll t; cin >> t;
    while(t--){ 
        ll n, m; cin >> n >> m;
        ll nn = n;
        // n = (n+m)|(n+1);
        // if(m==0)cout << nn << endl;
        // else 
        // cout << n << endl;
        while(m!=0){
            
        }
    }
    return 0;
}